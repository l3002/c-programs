#include <stdio.h>

int main(){
  int t;
  scanf("%d",&t);
  while(t-- > 0){
    int n,k;
    scanf("%d %d",&n,&k);
    long long h[n];
    long long p[n];
    for(int i=0; i<n; ++i){
      
    }
  }
  return 0;
}

